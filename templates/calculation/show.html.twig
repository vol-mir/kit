{% extends 'base.html.twig' %}

{% block title %}{{ 'title.page.show.calculation'|trans }}: {{ calculation.name }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">

            <div class="card card-primary card-outline card-outline-tabs">
                <div class="card-body">

                    <dl>
                        {% if calculation.name %}
                            <dt>{{ 'label.name'|trans }}</dt>
                            <dd>{{ calculation.name }}</dd>
                        {% endif %}
                    </dl>

                </div>
                <!-- /.card -->
            </div>

        </div>
    </div>
    <!-- /.row -->

{% endblock %}


{% block javascripts %}


{% endblock %}