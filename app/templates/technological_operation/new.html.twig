{% extends 'base.html.twig' %}

{% block title %}{{ 'title.page.new.technological_operation'|trans }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <!-- jquery validation -->
            <div class="card card-default">
                <!-- form start -->
                <form id="technologicalOperationForm" action="{{ path('technological_operation_new') }}" method="POST">
                    {% include 'technological_operation/_form.html.twig' with {'form': form} only %}
                    <div class="card-footer">
                        {{ form_widget(form.save) }}
                        {{ form_widget(form.saveAndCreateNew) }}
                        <a href="{{ path('technological_operation_index') }}"
                           class="btn btn-default float-right">{{ 'title.cancel'|trans }}</a>
                    </div>
                </form>
            </div>
            <!-- /.card -->
        </div>
    </div>
    <!-- /.row -->

{% endblock %}


{% block javascripts %}
    <!-- jquery-validation -->
    <script src="{{ asset('plugins/jquery-validation/jquery.validate.min.js') }}"></script>
    <script src="{{ asset('plugins/jquery-validation/additional-methods.min.js') }}"></script>

    <script type="text/javascript">
        $(document).ready(function () {

            $('#technologicalOperationForm').validate({
                rules: {
                    'technological_operation[name]': {
                        required: true,
                        maxlength: 190
                    },
                    'technological_operation[code_operation]': {
                        required: false,
                        maxlength: 190
                    },
                    'technological_operation[type_operation]': {
                        required: false,
                        maxlength: 190
                    },
                    'technological_operation[code_type_operation]': {
                        required: false,
                        maxlength: 190
                    },
                    'technological_operation[note]': {
                        required: false,
                        maxlength: 190
                    },
                },
                messages: {
                    'technological_operation[name]': {
                        required: "{{ 'message.validation.required'|trans }}",
                        maxlength: "{{ 'message.validation.too_long_content'|trans({ '%limit%': 190 }) }}"
                    },
                    'technological_operation[code_operation]': {
                        maxlength: "{{ 'message.validation.too_long_content'|trans({ '%limit%': 190 }) }}"
                    },
                    'technological_operation[type_operation]': {
                        maxlength: "{{ 'message.validation.too_long_content'|trans({ '%limit%': 190 }) }}"
                    },
                    'technological_operation[code_type_operation]': {
                        maxlength: "{{ 'message.validation.too_long_content'|trans({ '%limit%': 190 }) }}"
                    },
                    'technological_operation[note]': {
                        maxlength: "{{ 'message.validation.too_long_content'|trans({ '%limit%': 190 }) }}"
                    },
                },
                errorElement: 'div',
                errorPlacement: function (error, element) {
                    error.addClass('invalid-feedback');
                    element.closest('.form-group').append(error);
                },
                highlight: function (element, errorClass, validClass) {
                    $(element).addClass('is-invalid');
                },
                unhighlight: function (element, errorClass, validClass) {
                    $(element).removeClass('is-invalid');
                }
            });
        });
    </script>

{% endblock %}